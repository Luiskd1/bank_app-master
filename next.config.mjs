/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['source.unsplash.com', 'avatar.iran.liara.run', 'i.pravatar.cc','picsum.photos'],
    },
  };
  

export default nextConfig;
